<Pagination
	size="small"
	total={85}
	showTotal={total => `Total ${total} items`}
	pageSize={20}
	defaultCurrent={1}
	showSizeChanger
	showQuickJumper
	onChange={() => { }}
	onShowSizeChange={() => { }}
	simple={false}		// 简单模式
/>;
